/** Firebase project metadata — keep in sync with `.firebaserc` and downloaded SDK configs. */
export const firebaseConfig = {
  projectId: 'occasio-app-dev',
  region: 'asia-south1',
  /** Web client ID (client_type 3) — required for Google Sign-In on Android. */
  googleWebClientId:
    '751525617274-0t0ddniivdckvsed99l7s66v3jlvnv3f.apps.googleusercontent.com',
} as const;
